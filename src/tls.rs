// TLS support is available behind the `tls` feature flag.
// Enable with: cargo build --features tls
//
// When enabled, the server generates a self-signed CA + server cert on first run.
// The CA cert must be distributed to clients for verification.
